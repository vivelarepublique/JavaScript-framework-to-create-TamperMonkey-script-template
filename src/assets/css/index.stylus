@keyframes framework-test-stylus-animation
  from
    transform: skew(0deg, 0deg);

  50%
    transform: skew(20deg, 20deg);

  to
    transform: skew(0deg, 0deg);

img.framework-test-logo.framework-test-stylus-logo
  animation: framework-test-stylus-animation 5s linear infinite;
